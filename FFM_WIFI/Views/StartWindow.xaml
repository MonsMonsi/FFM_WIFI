<Window x:Class="FFM_WIFI.Views.StartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FFM_WIFI"
        mc:Ignorable="d"
        Title="Start-Menu" Height="700" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <GroupBox Header="Login" Grid.Row="1">
            <StackPanel>
                <TextBlock Text="Benutzername:" 
                           Margin="0,20,0,0"/>
                <TextBox Text="{Binding UserName}"/>
                <TextBlock Text="Passwort:" 
                           Margin="0,10,0,0"/>
                <TextBox Text="{Binding UserPassword}"/>
                <Button Content="Anmelden" Command="{Binding LoginCommand}"
                        Margin="0,30,0,0" Height="20"/>
                <Button Content="Neuer Benutzer" Command="{Binding NewUserCommand}" 
                        Margin="0,10,0,0" Height="20"/>
            </StackPanel>
        </GroupBox>
        <GroupBox Header="Datenbank" Grid.Row="2">
            <StackPanel>
                <Button Content="DB bearbeiten" 
                        Margin="0,70,0,0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="20"
                        Command="{Binding EditDBCommand}"/>
                <Button Content="DB erweitern" 
                        Margin="0,20,0,0" Height="20"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
