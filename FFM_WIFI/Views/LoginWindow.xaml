<Window x:Class="FFM_WIFI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FFM_WIFI.Views"
        mc:Ignorable="d"
        Title="LoginWindow" Height="500" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="93*"/>
            <RowDefinition Height="149*"/>
        </Grid.RowDefinitions>
        <GroupBox Header="Login" Grid.Row="1" Margin="0,35,0,0">
            <StackPanel>
                <TextBlock Text="Benutzername:" 
                           Margin="0,20,0,0"/>
                <TextBox Text="{Binding UserName}"/>
                <TextBlock Text="Passwort:" 
                           Margin="0,10,0,0"/>
                <TextBox Text="{Binding UserPassword}"/>
                <Button Style="{StaticResource ButtonLogin}"  Content="Anmelden" Command="{Binding LoginCommand}"
                        Margin="0,30,0,0" Height="20"/>
                <Button Style="{StaticResource ButtonLogin}" Content="Neuer Benutzer" Command="{Binding NewUserCommand}" 
                        Margin="0,10,0,0" Height="20"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
