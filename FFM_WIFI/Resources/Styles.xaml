<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style BasedOn="{StaticResource {x:Type Button}}"
           TargetType="{x:Type Button}"
           x:Key="Button">
        <Style.Setters>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                CornerRadius="7" BorderBrush="DarkBlue"  BorderThickness="2"
                                Background="LightBlue" TextBlock.Foreground="DarkBlue">

                            <TextBlock Text="{TemplateBinding Property=Content}"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                           FontSize="14" FontWeight="DemiBold" 
                                           Background="Transparent">

                                <TextBlock.Style>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Style.Triggers>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Opacity" Value="0.3"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>



                        <!--<Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

                                <TextBlock Text="{TemplateBinding Property=Content}"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                           FontSize="14" FontWeight="DemiBold" 
                                           Background="Transparent" Foreground="DarkBlue">

                                    <TextBlock.Style>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Style.Triggers>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Opacity" Value="0.3"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>-->

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>
    </Style>

    <Style TargetType="{x:Type TextBox}"
           x:Key="TextBoxUserName">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}" >
                    <Border CornerRadius="5" BorderThickness="2" BorderBrush="DarkBlue"
                            Height="25" Width="200"
                            Background="LightBlue">

                        <Grid>
                            <TextBox x:Name="UserNameBox"
                                     BorderThickness="0"
                                     Text="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type TextBox}}, Path=Text, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"
                                     HorizontalAlignment="Stretch" VerticalContentAlignment="Center"
                                     FontSize="14" FontWeight="DemiBold" 
                                     Background="Transparent" Foreground="DarkBlue"/>

                            <TextBlock IsHitTestVisible="False"
                                       Text="Username"
                                       HorizontalAlignment="Left" VerticalAlignment="Center"
                                       FontSize="12" FontWeight="Light" 
                                       Background="Transparent" Foreground="MediumBlue">

                                <TextBlock.Style>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, ElementName=UserNameBox}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                        <Setter Property="Visibility" Value="Hidden"/>
                                    </Style>
                                </TextBlock.Style>

                            </TextBlock>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type TextBox}"
           x:Key="TextBoxPassword">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border CornerRadius="5" BorderThickness="2" BorderBrush="DarkBlue"
                            Height="25" Width="200"
                            Background="LightBlue">

                        <Grid >
                            <TextBox x:Name="PasswordBox"
                                     BorderThickness="0"
                                     Text="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type TextBox}}, Path=Text, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"
                                     HorizontalAlignment="Stretch" VerticalContentAlignment="Center"
                                     FontSize="14" FontWeight="DemiBold" 
                                     Background="Transparent" Foreground="DarkBlue"/>

                            <TextBlock IsHitTestVisible="False"
                                       Text="Password"
                                       HorizontalAlignment="Left" VerticalAlignment="Center"
                                       FontSize="12" FontWeight="Light" 
                                       Background="Transparent" Foreground="MediumBlue">

                                <TextBlock.Style>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, ElementName=PasswordBox}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                        <Setter Property="Visibility" Value="Hidden"/>
                                    </Style>
                                </TextBlock.Style>

                            </TextBlock>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--<Style TargetType="{x:Type DataGridCell}" BasedOn="{StaticResource {x:Type DataGridCell}}"
           x:Key="DataGridCellPlayer">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->

    <!--<Style BasedOn="{StaticResource x:Type ListView}"
           x:Key="ListViewTest">
        <Setter Property="Cont">
            <Setter.Value>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background" Value="LightBlue"/>
                    <Setter Property="BorderThickness" Value="5"/>
                    <Setter Property="BorderBrush" Value="DarkBlue"/>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>-->
</ResourceDictionary>